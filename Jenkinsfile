library('pipeline')_
DockerPipeline {
  imageName = "n9-node-microservice-skeleton" // todo on init skeleton: rename by your api name
	project = "project-name"
	tests = ["ls -la"]
	deployment = false
}
